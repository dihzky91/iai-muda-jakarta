# Optional: Gemini API key for future AI features.
# This project can run locally without setting it.
GEMINI_API_KEY=""

# NEXT_PUBLIC_APP_URL: The URL where this applet is hosted (client-side).
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Database connection (TiDB Cloud Serverless / MySQL)
# Untuk TiDB Cloud Serverless: port 4000, wajib SSL
DB_HOST="localhost"
DB_USER="root"
DB_PASSWORD=""
DB_NAME="iai_muda_jakarta"
DB_PORT="3306"
# DATABASE_URL="mysql://user:password@host:4000/iai_muda_jakarta?ssl={}"

# Cloudinary — untuk penyimpanan foto/gambar
# Format: cloudinary://<api_key>:<api_secret>@<cloud_name>
CLOUDINARY_URL=cloudinary://your_api_key:your_api_secret@your_cloud_name

# JWT Auth — change this to a long random string in production!
JWT_SECRET="change-this-to-a-long-random-secret-in-production"
JWT_EXPIRES_IN="8h"

# Initial superadmin password (used by npm run seed)
SUPERADMIN_PASSWORD="change-this-password"
